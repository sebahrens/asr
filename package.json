{
  "name": "asr",
  "version": "0.1.0",
  "description": "Self-hosted Agent Skills Registry",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:api": "node deploy/docker/dev-api.mjs",
    "build": "turbo build",
    "lint:e2e": "node ./scripts/check-e2e-specs.mjs && node ./scripts/check-docker-compose-security.mjs",
    "test": "pnpm lint:e2e && pnpm --filter @asr/core test && pnpm --filter @asr/cli test",
    "test:e2e": "node ./scripts/e2e-docker.mjs",
    "cli": "pnpm --filter @asr/cli",
    "web": "pnpm --filter @asr/web"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "turbo": "^2.9.16",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.6.0"
  }
}
