// Visible product/title name (page title, hero heading, accessible link names).
export const PRODUCT_NAME = 'asr';

// Compact wordmark shown as the top-bar logo ("| asr").
export const PRODUCT_WORDMARK = 'asr';
